/** @flow */

import { execute } from './main';

describe('Bank', () => {
  it('should throw an error', () => {
    // Given

    // When

    // Then
    expect(() => {
      execute();
    }).toThrowError(
      'Not implemented yet'
    );
  });
});
