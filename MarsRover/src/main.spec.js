/** @flow */

import { execute } from './main';

describe('Rover', () => {
  it('should move forward when it receives the command "????"', () => {
    expect(execute).toThrow('Not implemented yet');
  });
});
